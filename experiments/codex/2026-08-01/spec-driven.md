# 仕様を作ってから実装する

## 実行条件

| 項目 | 内容 |
| --- | --- |
| 開始コミット | `af3da8d` |
| Codex CLI | 0.145.0 |
| モデル | `gpt-5.6-sol`（reasoning low） |
| 権限 | Workspace、必要時に承認 |
| 初回の入力 | `spec.md`のPlanレビュー |
| テスト仕様 | 提供しない |
| 共通指示 | `AGENTS.md` |
| Planレビュー | 1分6秒 |
| 実装 | 2分9秒 |
| 合計 | 3分15秒 |
| 修正の追加指示 | なし |
| 利用量 | 非キャッシュ入力62,925、キャッシュ入力331,264、出力11,898トークン |

費用はChatGPTプラン内の利用で金額表示がないため、推定していません。利用量はPlanレビューと実装を含む同一セッションの記録です。

## Planレビュー

Codexからの確認質問はありませんでした。800ミリ秒の境界、判定待ち中のリセット、再シャッフルの保証範囲、クリア後の状態不変を明確にする指摘を採用し、実装前に`spec.md`を更新しました。

クラス名や固定山札のデータ形式など、実装方法を固定する指摘は採用しませんでした。詳細は`spec-review.md`に記録しています。

## 実装結果

ゲーム状態を`MemoryGame`、DOM操作を`main.ts`へ分離しました。固定山札、799ミリ秒と800ミリ秒の境界、判定待ち中のリセット、クリア後の状態不変を含む7件のテストを作成しました。

仕様の完了条件には`test.md`への参照が残っていましたが、この方式では`test.md`を提供していません。Codexは存在しないことを報告し、代わりに`spec.md`からテストを作成しました。

Codexはブラウザ操作を実施せず、人が確認する項目として正確に報告しました。確認質問とコマンド承認要求はありませんでした。

## 最終検証

- Node.js 24 Dev Containerで型検査、7件のテスト、ビルドが成功
- ブラウザで16枚の4行4列表示、手数、不一致後の復帰を確認
- ブラウザで探索を含む14手ですべてを一致させ、ゲームクリア表示と最終手数を確認
- リセット後に手数0と16枚の裏向き状態へ戻ることを確認
- 裏向き、表向き、一致済みを区別する支援技術向けラベルを確認
- 320ピクセル幅で横スクロールが発生しないことを確認
- ブラウザのコンソール警告・エラーなし

キーボード操作にはネイティブの`button`要素とフォーカス表示を使用しています。ブラウザ制御機能ではEnterキーの発火を確認できなかったため、キーボードだけでゲームを完了する確認は未実施です。
